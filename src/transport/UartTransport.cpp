#include "transport/UartTransport.hpp"


void UartTransport::connect() {
    // Implementation for connecting to UART
}

void UartTransport::disconnect() {
    // Implementation for disconnecting from UART
}

void UartTransport::sendData(const char* data, size_t size) {
    // Implementation for sending data over UART
}

void UartTransport::receiveData(char* buffer, size_t size) {
    // Implementation for receiving data over UART
}